// 10 '10'
type MovieId = number | string
type Movie = string

let movieId: MovieId
movieId = 10
movieId = '10'

// BUscar un use dado un id
function getMovienameById (id: MovieId): Movie {
  // find the user
  return 'aomine'
}

getUsernameById(20)
getUsernameById('20')
