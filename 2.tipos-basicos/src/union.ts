// 10 '10'
let userId: number | string
userId = 10
userId = '10'

// BUscar un use dado un id
function getUsernameById (id: number | string) {
  // find the user
  return 'aomine'
}

getUsernameById(20)
getUsernameById('20')
